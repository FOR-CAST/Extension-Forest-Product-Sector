# SimpleTier1 with BioToFPS written BioToFPSX, which is not a pool name at all.
# RECORDS CURRENT BEHAVIOUR, which is wrong: indistinguishable from the
# wrong-case version above, because both fall through GetFPSPool() to 4.
exit_zero: yes
log_contains: No proportions found for Forest To Mills
