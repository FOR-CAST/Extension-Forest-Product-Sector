# SimpleTier1 with BioToFPS written BioToFPSX, which is not a pool name.
# An unmatched name now fails the run instead of silently becoming an
# out-of-range code that nothing ever requests.
exit_zero: no
stdout_contains: is not a valid pool name
